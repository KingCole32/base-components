export type ColumnDef = {
  name: String
  isSortable?: Boolean
  isVisible?: Boolean
  alignment?: String
}

// also should contain other types associated with base components, e.g. SelectOptions, for potential dropdown menus
